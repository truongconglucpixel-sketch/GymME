<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GymMentor">

        <activity
            android:name="com.example.gymmentor.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.GymMentor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".timer.ExerciseLibraryActivity" android:exported="false" />
        <activity android:name=".timer.TimerActivity" android:exported="false" />
        <activity android:name=".timer.ExerciseDetailActivity" android:exported="false" />
        <activity android:name=".timer.RoutineListActivity" android:exported="false" />
        <activity android:name=".timer.RoutineDetailActivity" android:exported="false"/>
        <activity android:name=".timer.LiveWorkoutActivity" android:exported="false"/>
    </application>
</manifest>